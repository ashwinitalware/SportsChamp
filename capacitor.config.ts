import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sportschamp.com',
  appName: 'SportsChamp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
